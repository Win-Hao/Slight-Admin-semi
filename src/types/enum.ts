export enum ThemeColorPresets {
    Default = 'default',
    Cyan = 'cyan',
    Purple = 'purple',
    Blue = 'blue',
    Orange = 'orange',
    Red = 'red',
}

export enum ThemeMode {
    Light = 'light',
    Dark = 'dark',
}

export enum PermissionType {
    CATALOGUE,
    MENU,
}